extends base.pug
block style
    style
        include ../static/css/stylee.css
        include ../static/css/login.css
block container
    .container
        form.myloginForm(action='/l', method='post')
            //- p.login LOGIN
            h5 LOGIN
            p.logintext Login to make purchase
            input.myInput(type="text" name="email" placeholder="Enter your email..")
            input.myInput(type="password" name="password" placeholder="Enter your password..")
            button.btn1(value="submit")
                |   Submit   